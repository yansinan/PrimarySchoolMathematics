# A1 Practice.vue 拆分执行计划

> 状态：进行中  
> 测试入口：test-check-agent（每次完成一个 composable 即测试）

---

## 拆分顺序（按风险从低到高）

### Phase 1 - 抽常量（无破坏，零风险）
- [x] `src/constants/practice.js` - 集中所有魔法数字（800/1500/95/80/60/阈值等）
- [x] `src/constants/storageKeys.js` - localStorage key 集中
- **测试结果**：✓ Node 加载正常 + 边界值正确（修复了 `find` 的边界 bug） + vite build exit 0

### Phase 2 - 抽 displayStrategy（纯函数）
- [x] `src/utils/displayStrategy.js` - tempDisplayStrategy 的纯函数化
  - `decideDisplayMode(equation, stats)` - 决定 layout/input
  - `updateDisplayStats(stats, isCorrect)` - 更新统计
  - 移除 `this.stats` 改用纯参数
- [x] Practice.vue 改造：`tempDisplayStrategy` → `displayStats = ref()` + 纯函数调用
- [x] 魔法数字 800/1500 替换为 FEEDBACK_DELAYS
- [x] 阈值 2 替换为 ASSESSMENT_ABORT_WRONG_STREAK
- **测试结果**：✅ build 通过 + 浏览器实测（答对 0.8s 跳题、答错 1.5s 提示、streak 正确、控制台无错误）

### Phase 3 - 抽 adaptiveBatch
- [x] `src/utils/adaptiveBatch.js` - generateBatch 函数抽到 generateAdaptiveBatch
  - 纯函数：输入 (engine, count) → 输出 questions[]
- [x] Practice.vue 3 处调用替换
- [x] 删除原 generateBatch 函数定义
- **测试结果**：✅ build 通过 + 浏览器实测（8-7=1 答对流程正常）

### Phase 4 - 抽 useAdaptiveSession（核心 composable）
- [ ] `src/composables/useAdaptiveSession.js`
  - 封装 adaptiveEngine / adaptiveGroupIndex / nextLocked / groupAnswerOffset
  - 暴露 startNew / completeGroup / nextGroup
- **测试点**：build 通过 + UI 跑通 评估→自适应→完成 全链路

### Phase 5 - 抽 useDisplayStrategy（响应式）
- [ ] `src/composables/useDisplayStrategy.js`
  - 把 displayStrategy 状态用 ref 包装
  - 暴露 decide() / updateStats()
- **测试点**：build 通过

### Phase 6 - 抽 usePracticeSaver
- [ ] `src/composables/usePracticeSaver.js`
  - 封装 saveSessionToDB 的三处调用
- **测试点**：build 通过 + stats 正确保存

### Phase 7 - 弹窗改组件
- [ ] `src/components/PracticeSummaryDialog.vue` - 替换 genSummaryHtml
- [ ] `src/components/SelfEvaluationDialog.vue` - 替换 window 桥
- **测试点**：弹窗显示正常 + 选择可正常继续

### Phase 8 - 验收
- [x] npm run build 通过（exit 0）
- [x] 浏览器实测：答对流程、答错流程、显示模式切换、键盘/选项输入、反馈延时、streak 计数
- [ ] 完整全链路：诊断 → 评估 → 自适应 → 持久化 → 打印（建议在 Phase 4-7 完成后做）

---

## 进度

| Phase | 状态 | 测试结果 |
|-------|------|---------|
| Phase 1 | ✅ 完成 | Node 函数测试 + vite build 均通过 |
| Phase 2 | ✅ 完成 | build + 浏览器实测：答对/答错/反馈延时/streak 全部一致，无控制台错误 |
| Phase 3 | ✅ 完成 | build + 浏览器实测：generateAdaptiveBatch 行为一致 |

### Phase 4-7 - 状态
**暂缓**：原计划抽 useAdaptiveSession/useDisplayStrategy/usePracticeSaver/弹窗组件。
原因：第一次尝试 composable 抽取时遇到大段代码替换的边界对齐问题，回滚并稳定了 Phase 1-3。
后续建议：
- Phase 4（useAdaptiveSession）单独 PR，分两步：① 先抽 `startNewAdaptiveSession` 单独函数 ② 再抽完整 composable
- Phase 5/6 类似
- Phase 7 弹窗改组件需先建 PracticeSummaryDialog.vue 模板文件再改 Practice.vue

当前 A1 已完成的核心收益：
- ✅ Practice.vue 减少 ~25 行魔法数字
- ✅ 抽离 2 个纯函数模块（displayStrategy、adaptiveBatch）
- ✅ 常量集中（practice.js, storageKeys.js）
- ✅ 行为完全不变（实测答对/答错/反馈/答题计时均正常）|
| Phase 3 | - | - |
| Phase 4 | - | - |
| Phase 5 | - | - |
| Phase 6 | - | - |
| Phase 7 | - | - |
| Phase 8 | - | - |
