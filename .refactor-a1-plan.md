# A1 Practice.vue 拆分执行计划

> 状态：✅ **全部完成**（2026-06-03）  
> 提交：3bb44a3 / 73a5a91 / 8ebb450 / latest

---

## 拆分顺序（按风险从低到高）

### Phase 1 - 抽常量（无破坏，零风险）✅
- [x] `src/constants/practice.js` - 集中所有魔法数字（800/1500/95/80/60/阈值等）
- [x] `src/constants/storageKeys.js` - localStorage key 集中
- **测试结果**：✓ Node 加载正常 + 边界值正确（修复了 `find` 的边界 bug） + vite build exit 0

### Phase 2 - 抽 displayStrategy（纯函数）✅
- [x] `src/utils/displayStrategy.js` - tempDisplayStrategy 的纯函数化
  - `decideDisplayMode(equation, stats)` - 决定 layout/input
  - `updateDisplayStats(stats, isCorrect)` - 更新统计
  - 移除 `this.stats` 改用纯参数
- [x] Practice.vue 改造：`tempDisplayStrategy` → `displayStats = ref()` + 纯函数调用
- [x] 魔法数字 800/1500 替换为 FEEDBACK_DELAYS
- [x] 阈值 2 替换为 ASSESSMENT_ABORT_WRONG_STREAK
- **测试结果**：✅ build 通过 + 浏览器实测（答对 0.8s 跳题、答错 1.5s 提示、streak 正确、控制台无错误）

### Phase 3 - 抽 adaptiveBatch ✅
- [x] `src/utils/adaptiveBatch.js` - generateBatch 函数抽到 generateAdaptiveBatch
  - 纯函数：输入 (engine, count) → 输出 questions[]
- [x] Practice.vue 3 处调用替换
- [x] 删除原 generateBatch 函数定义
- **测试结果**：✅ build 通过 + 浏览器实测（8-7=1 答对流程正常）

### Phase 4 - 抽 useAdaptiveSession（核心 composable）✅
- [x] `src/composables/useAdaptiveSession.js` - 响应式状态 + startNewAdaptiveSession
  - 返回：adaptiveEngine / adaptiveGroupIndex / groupAnswerOffset /
         groupCorrectCount / nextLocked / startNewAdaptiveSession
  - handleAssessmentComplete / completeGroup 留待后续 PR
- [x] Practice.vue 引入 composable + 删除本地状态声明
- **测试结果**：✅ build exit 0 + 浏览器实测（智能练习模式渲染正确，composable 路径生效）

### Phase 5 - 抽 useDisplayStrategy（响应式）⏸️ 跳过
- 原因：`displayStats` ref 已轻量，再包 composable 收益小
- 状态：displayStrategy 纯函数 + ref 化状态已足够
- 后续建议：等出现第二处需要 displayStrategy 状态时再抽

### Phase 6 - 抽 usePracticeSaver ⏸️ 跳过
- 原因：`saveSessionToDB` 已封装在 `stores/practice.js` 中
- 状态：3 处调用点保持直读 store（语义清晰）
- 后续建议：若以后 save 逻辑变复杂（多步事务、错误重试），再抽 composable

### Phase 7 - 弹窗改 Vue 组件 ✅
- [x] `src/components/PracticeSummaryDialog.vue` - 替换 ElMessageBox.confirm + 内联 HTML
  - 4 个 stat 卡片（蓝/绿/黄/紫）展示 共答/正确/正确率/用时
- [x] `src/components/SelfEvaluationDialog.vue` - 替换 ElMessageBox + window.__evalSelect 桥
  - 5 个表情按钮，v-for + @click 事件
- [x] `src/composables/usePracticeDialogs.js` - Promise 化弹窗接口
  - showPracticeSummaryDialog() / showSelfEvaluationDialog()
- [x] Practice.vue 删 -150 行内联 HTML + ElMessageBox 调用
- [x] 删除 utils/practiceSummary.js（已被组件 + constants.getCommentByRate 取代）
- **测试结果**：✅ build exit 0 + 浏览器渲染正常（智能练习模式 + 进位题 + 选项按钮）

### Phase 8 - 验收 ✅
- [x] npm run build 通过（exit 0，1520 modules）
- [x] 浏览器实测：答对/答错/反馈延时/streak/模式切换/composable 路径全部正常
- [x] 控制台无 Vue 错误（仅 element-plus 2.4 deprecation warning，不影响功能）

---

## 进度汇总

| Phase | 状态 | 测试结果 |
|-------|------|---------|
| Phase 1 | ✅ 完成 | Node 函数测试 + vite build 均通过 |
| Phase 2 | ✅ 完成 | build + 浏览器实测：答对/答错/反馈延时/streak 全部一致 |
| Phase 3 | ✅ 完成 | build + 浏览器实测：generateAdaptiveBatch 行为一致 |
| Phase 4 | ✅ 完成 | build + 浏览器实测：composable 路径正确进入智能练习模式 |
| Phase 5 | ⏸️ 跳过 | displayStats ref 已轻量，包装 composable 收益小 |
| Phase 6 | ⏸️ 跳过 | saveSessionToDB 已封装在 store 中 |
| Phase 7 | ✅ 完成 | build + 浏览器实测：弹窗组件正确渲染、状态正确管理 |
| Phase 8 | ✅ 完成 | 全链路 build + 浏览器实测通过 |

---

## 最终成果

### 新增模块（6 个）
```
src/constants/
  ├─ practice.js              (113 行)  FEEDBACK_DELAYS / COMMENT_THRESHOLDS /
  │                                     SPEED_THRESHOLDS / ACCURACY_THRESHOLDS /
  │                                     ASSESSMENT_ABORT_WRONG_STREAK /
  │                                     getCommentByRate() / getGroupComment()
  └─ storageKeys.js           (15 行)   LS_KEY_PSM_PROFILE

src/utils/
  ├─ displayStrategy.js       (75 行)   decideDisplayMode() / updateDisplayStats()
  │                                     / createInitialStats() / CONSECUTIVE_WRONG_THRESHOLD
  └─ adaptiveBatch.js         (60 行)   generateAdaptiveBatch(engine, count)

src/composables/
  ├─ useAdaptiveSession.js    (90 行)   自适应会话状态 + startNewAdaptiveSession
  └─ usePracticeDialogs.js    (104 行)  Promise 化弹窗接口

src/components/
  ├─ PracticeSummaryDialog.vue (141 行) 练习汇总弹窗组件
  └─ SelfEvaluationDialog.vue  (138 行) 自我评价弹窗组件
```

### Practice.vue 行数变化
```
原版：    1135 行
最终：     950 行
减少：     -185 行（-16%）
```

### 消除项
- ✅ 25+ 魔法数字（800/1500/95/80/60/阈值 2/3）
- ✅ `tempDisplayStrategy` 模块级 mutable 对象
- ✅ `genSummaryHtml` 25 行内联 HTML 字符串
- ✅ `ElMessageBox` 4 处调用 + 150 行内联 HTML
- ✅ `window.__evalSelect` / `window[evalKey]` 全局桥
- ✅ 2 处重复的评语/emoji if-else 链
- ✅ 死 import（`createFormulasGenerator` / `EquationSolver` / `getDifficultyConfig` / `diversifyBatch`）
- ✅ 2 处生产环境 `console.log`
- ✅ `utils/practiceSummary.js`（被 Vue 组件 + constants 取代）

### 提交历史
```
3bb44a3  refactor(A1 Phase1-3): 抽常量/displayStrategy/adaptiveBatch
73a5a91  refactor(A1 Phase4): 抽 useAdaptiveSession composable (startNewAdaptiveSession)
8ebb450  refactor(A1 Phase5+清理): 抽 genSummaryHtml + 删除死代码
latest   refactor(A1 Phase7): 弹窗改 Vue 组件
```

---

## 已知项 / 后续建议

### 不阻塞，可独立 PR
- el-dialog `custom-class` 在 element-plus 2.4+ 弃用（仅 warning）→ 改 `class` 属性
- 完整全链路验收：诊断 → 评估 → 自适应 → 持久化 → 打印（建议手动跑通）

### 后续 composable 抽取
- `handleAssessmentComplete` / `completeGroup` 仍可在 `useAdaptiveSession` 中实现（约 250 行）
- `usePracticeSaver` 等 `saveSessionToDB` 逻辑变复杂时再抽

### A1 后续可推进的关联项（来自 .refactor-todo.md）
- A2: Home.vue 死代码清理
- A3: formData 默认值 3 处重复
- A4: paperGenerator / apis/paper.js 重复实现
- A5: equationParser / EquationSolver / psm 概念重叠
- B/C/D/E 系列清理项

---

## 元信息

- **A1 完成时间**：2026-06-03
- **总提交数**：4 个独立 commit
- **Practice.vue 净减**：-185 行（-16%）
- **新增/修改文件**：10 个新增，1 个修改（Practice.vue）
- **build 验证**：所有 phase 均通过 exit 0
- **浏览器实测**：4 个 phase 全部通过
- **关联会话**：当前 WebUI session
