# Primary School Mathematics — 设计文档

## 设计哲学

### 核心理念
> **保持信心，探索边界。** 每个学生都有自己的节奏，系统要做的是在舒适区与挑战区之间找到最优平衡点。

### 设计原则
1. **从易到难** — 能力评估从 L1（个位数简单加减）逐步过渡到 L5（混合题型），让学
   生建立信心。
2. **粒度精细** — 难度从「起步」到「综合挑战」共 16 级，每步变化微小，学习曲线
   平缓。
3. **及时反馈** — 每完成一组练习，系统给出总结，并邀请学生自我评价。
4. **自适应调节** — 基于客观表现（正确率、速度）+ 主观感受（自我评价）共同决定
   下一组配置。
5. **多样题型** — 同一组内混合键盘输入、选择题、混合填空位置，避免枯燥。

---

## 一、能力评估设计

### 流程
```
首次进入 → 自动生成 5 题诊断 → 答题 → 分析能力 → 启动自适应练习
连续错 2 题 → 提前结束评估（剩余等级自动标记为弱项）
```

### 难度等级

| 等级 | 范围 | 运算符 | 规则 | 题数 |
|------|------|--------|------|------|
| L1 | 1-9 | +/− | 和 ≤ 10，无退位 | 1 |
| L2 | 2-20 | +/− | 可进位/退位 | 1 |
| L3 | 10-99 | +/− | 无进退位 | 1 |
| L4 | 10-99 | +/− | 有进退位 | 1 |
| L5 | 混合 | +/− | 各种类型 | 1 |

### 分析逻辑
`analyzeAbility(answers)` — 按等级计算正确率，低于 50%（全错）标记为弱项。

---

## 二、三维自适应引擎

引擎同时调节三个独立维度，粒度远超市面上多数练习产品。

### 维度 1：难度阶梯（16 级）

| 阶段 | 级别数 | 数字范围 | 进退位 | 典型标签 |
|------|--------|----------|--------|----------|
| 建立信心 | 3 | 1-5 → 1-9 | 禁止 | 起步 / 巩固 / 进阶 |
| 引入进退位 | 2 | 2-9 | 混合→鼓励 | 混合进退位①② |
| 过渡到两位数 | 2 | 10-50 | 禁止 | 两位数入门/巩固 |
| 两位数进退位 | 2 | 11-99 | 混合 | 进退位入门/巩固 |
| 大范围 | 3 | 50-999 | ±分科 | 大数加法/减法/综合 |

### 维度 2：输入辅助模式（3 级）

| 模式 | 组内占比 | 说明 |
|------|----------|------|
| `keypad`（键盘） | 标准模式 70%+ | 直接输入数字 |
| `choice2`（二选一） | 中等辅助 55%+ | 两个选项中选择 |
| `choice4`（四选一） | 高辅助 65%+ | 四个选项中选择 |

**调节逻辑**：连续 2 组成绩差 → 提升辅助级别；连续 3 组成绩好 → 降低辅助级别。

### 维度 3：填空位置模式（2 种）

| 模式 | 组内占比 | 题例 | 难度 |
|------|----------|------|------|
| `result`（结果填空） | 85% | `3+5=__` | 标准 |
| `mixed`（混合填空） | 15% | `__+5=8` 或 `3+__=8` | 更难 |

**调节逻辑**：连续 3 组又好又快 → 尝试混合填空；成绩下滑 → 回到 result。

### 辅助与填空的穿插策略

即使引擎处于「键盘 + result」的基准状态，每道题仍有概率出现不同模式：
- 20% 概率出现选择题（二选一或四选一），保持新鲜感
- 15% 概率出现混合填空，轻度增加思维挑战

这种**概率穿插**机制确保同一组内的题型始终多样。

---

## 三、题组设计

### 分组练习
- 题目不再一次性全部展示，而是分为**若干小组**
- 每组题量从少到多：`[4, 5, 6, 7, 8, 10, 12]`
- 正确的多且快 → 组量递增；错误多或慢 → 组量递减

### 练习量范围
- 最少 `targetMin`（默认 10）→ 保证基本练习量
- 最多 `targetMax`（默认 30）→ 封顶，不会无限制
- **表现好**（连续多组又快又好）→ 在 min ~ max 间提前结束
- **表现差**（连续差且超 min+5）→ 软结束避免挫败

### 组间反馈
每组完成后，系统会：
1. 显示本组统计数据（正确数、用时）
2. **弹出自我评价对话框**，学生选择 5 级表情
3. 评价影响下一组的配置（见下文）

---

## 四、自我评价 & 反馈闭环

### 五级评价

| 表情 | 评分 | 含义 | 对引擎的影响 |
|------|------|------|-------------|
| 😭 | 1 | 很难 | 保守：升级门槛提高，题量不减 |
| 😟 | 2 | 有点难 | 保守：升级需更多好成绩 |
| 😊 | 3 | 还好 | 中性：标准策略 |
| 😂 | 4 | 轻松 | 激进：升级门槛降低，题量更易减 |
| 😌 | 5 | 很轻松 | 激进：更容易升级，更快减量 |

### 评分对决策的量化影响

```
effectiveGood = GOOD - (selfEval - 3) * 0.15
effectiveBad  = BAD  - (selfEval - 3) * 0.075

selfEval 1 → 升级需连续 4 组好成绩，不增加题量
selfEval 5 → 升级只需 2 组好成绩，快速减少题量
```

### 数据持久化
- 评价记录存储在 DB 的 `practiceSessions.evaluations` 字段（JSON）
- 格式：`[{"group":1,"score":4}, {"group":2,"score":3}, ...]`

---

## 五、题型穿插机制

### 输入模式概率分配

| 引擎状态 | keypad | choice2 | choice4 |
|----------|--------|---------|---------|
| 键盘模式 (L=0) | 70% | 20% | 10% |
| 二选一模式 (L=1) | 20% | 55% | 25% |
| 四选一模式 (L=2) | 10% | 25% | 65% |

### 填空位置概率分配

| 引擎状态 | result | mixed |
|----------|--------|-------|
| result 模式 | 85% | 15% |
| mixed 模式 | 40% | 60% |

---

## 六、项目结构

### 关键文件

| 文件 | 职责 |
|------|------|
| `src/utils/diagnostic.js` | 能力评估引擎：生成诊断题、分析能力 |
| `src/utils/adaptiveEngine.js` | 自适应练习引擎：三维调节、组管理、结束决策 |
| `src/utils/paperGenerator.js` | 算式生成器：基于配置生成随机算式 |
| `src/utils/EquationSolver.js` | 方程求解器：支持 `__` 空白求解 |
| `src/utils/database.js` | IndexedDB 持久层：实践记录、评价存储 |
| `src/stores/practice.js` | Pinia Store：练习状态、答题记录、DB 保存 |
| `src/components/Practice.vue` | 练习主界面：答题交互、组管理、评价对话框 |
| `src/components/Generate.vue` | 练习配置抽屉：参数设置、目标范围 |
| `src/components/home/AutoGenerateFormulas.vue` | 自动出题配置表单 |

### 数据流

```
配置参数 → paperGenerator → 算式列表 → EquationSolver求解
  → diversifyBatch (穿插题型)
    → Practice.vue (答题交互)
      → evaluateGroup (评估表现)
        → 下一组配置 + 是否结束
          → 结束 → saveSessionToDB (含评价)
```

---

## 七、未来扩展方向

1. **云端同步** — 预留了 `synced` / `updatedAt` 字段，可对接 Dexie Cloud
2. **乘除法支持** — 当前引擎专注于加减法，可扩展乘除法难度阶梯
3. **竖式计算** — 竖式布局已支持，可丰富竖式题型
4. **定时模式** — 可增加限时挑战模式
5. **多用户** — studentId 字段已预留

### 个性化速度基准（待实现）

当前系统的速度等级判断使用了全局固定阈值（`<3s/+2, <5s/+1, <8s/0, <12s/-1, ≥12s/-2`）。随着用户做题数据的积累，应该切换到**个性化基准线**：

**思路**：
1. 按难度级别（或难度级别+运算符）统计每个用户的历史平均用时，作为该用户的"正常"速度基准
2. 实际做题速度与个人基准线比较来判断快慢，而非全局固定值
3. 例如：某用户两位数加减平均 8s，则对 ta 来说 6s 算"快"、12s 算"慢"

**实现要点**：
- 从 `answers` 表中按 `(operator, operandMin, operandMax, stepCount)` 分组统计用户的历史 `responseTime` 均值
- 在 `createAdaptiveEngine()` 时加载该用户的个人基准数据
- 新用户的基准逐渐收敛——前期用全局阈值，3~5 组数据后切换到个人基线
- 基准需要定期重新计算（或每次练习后增量更新），以反映进步

```
// 伪代码示例
function getPersonalBaseline(studentId, difficultyCtx) {
  const history = db.answers
    .where({ studentId, operator: ctx.operator, stepCount: ctx.stepCount })
    .average('responseTime')
  return history || FALLBACK_THRESHOLD  // 数据不足时用全局阈值
}
```
