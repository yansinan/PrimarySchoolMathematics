// ───────────────────────────────────────────────────────────────────────
// PR 1.1 过渡 stub — 真实实现位于 ./algorithm/EquationSolver.js
//
// 用途：保持旧路径 utils/EquationSolver 可解析（Vite 按文件/目录解析，
// 不会回落到 utils/index.js 桶）。`export *` 风格透传所有命名导出。
//
// 当前 src/ 内无 import 引用此路径（PR 1.1 验证：grep 计数 0），但保留本
// stub 以防遗漏（外部脚本/测试可能依赖）。
//
// 后续 PR（1.2 重复消除 + 1.3 路径统一）会清理此类历史 stub。
// ───────────────────────────────────────────────────────────────────────
export * from './algorithm/EquationSolver'
