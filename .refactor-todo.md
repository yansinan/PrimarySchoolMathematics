# PrimarySchoolMathematics /src 查改清单

> 范围：src/** 下所有 Vue 组件、JS 工具、Pinia stores  
> 目标：定位散乱实现 + 重复逻辑 + 死代码 + 可复用机会  
> 优先级：🔴 高 → 🟡 中 → 🟢 低  
> 状态：待执行（截至 2026-06-02）

---

## A. 最高优先级（散乱严重 / 影响维护）

### A1. Practice.vue 巨型组件（🔴）
- **路径**：`src/components/Practice.vue`（1135 行）
- **现状**：UI 渲染 + 自适应引擎编排 + 计时 + 输入路由 + 评价弹窗生成 + 持久化全在一个文件
- **散乱清单**（按可复用模块划分）：
  - `tempDisplayStrategy`（133-159）+ `decide()/updateStats()` → 抽成 `composables/useDisplayStrategy.js` 或 `utils/displayStrategy.js`
  - `generateBatch()`（460-481）→ 抽到 `utils/adaptiveBatch.js`，与 `practice.js` 解耦
  - `handleAssessmentComplete()`（484-510）+ `startNewAdaptiveSession()`（722-758）+ `completeAdaptiveGroup()`（512-720）→ 抽成 `composables/useAdaptiveSession.js`
  - `genSummaryHtml()`（96-113）+ 评价 HTML（540-575）+ 完结 HTML（642-666）→ 抽成独立 Vue 组件 `PracticeSummaryDialog.vue` / `SelfEvaluationDialog.vue`；不要再用 `dangerouslyUseHTMLString`
  - `window.__evalSelect` / `window[evalKey]`（561-576）→ 桥接做法是反模式，迁到 scoped slot / 组件 emit
  - 时长常量 `800 / 1500` 等魔法数字 → 集中 `const FEEDBACK_DELAYS = { correct: 800, wrong: 1500 }`
  - `handleInput` 中 vertical/horizontal 两个分支（273-292）结构完全相同 → 合并
  - `bsLock` / `nextLocked` 模块级 mutable（172, 271）→ 用 `ref` 移进 composable
- **可复用模块**：
  - `utils/equationParser.js` 的 `parseEquation` / `getCarryType` 已存在
  - `utils/diagnostic.js` 的 `generateDiagnosticQuestions` / `analyzeAbility` / `generatePracticeConfig` 已存在
  - `utils/adaptiveEngine.js` 的 `createAdaptiveEngine` / `evaluateGroup` / `diversifyBatch` 已存在
  - `stores/practice.js` 的 `saveSessionToDB` / `setConfigSnapshot` / `startQuestionTimer` 已存在
- **复用机会**：上述 composable 全部基于现有 utils/stores，不要重新实现

### A2. Home.vue 几乎全是死代码（🔴）
- **路径**：`src/views/Home.vue`（203 行）
- **问题**：
  - 现状被 `views/Layout.vue` 取代（实际渲染走 `Generate` 抽屉），整个 `Home.vue` 是旧入口
  - `onMounted` 里的 `console.log('少年，我看你骨骼精奇…')`（93 行）彩蛋
  - `generate()` / `generateFormulas()`（157-201）：`generateFormulas` 末尾有 `debugger`（200 行），是开发残留
  - `import { download } from '@/utils/download'`（50 行）但 Home.vue 没有调用 → 死 import
  - `import { httpContentTypeExtensionsMappingEnum }`（49 行）同上
  - `import { generatePaper } from '@/apis/paper'`（51 行）同上
  - `formData` 默认值（61-88）与 `Generate.vue`（102-131）、`configStorage.js`（27-58）三处重复
  - `selectedConfiguration` 字段复制（132-152）与 `Generate.vue`（177-199）重复
- **建议**：
  1. 删除 `Home.vue`（路由若仍指向 `/home`，迁移到 `Layout.vue` 即可；或保留 Home 但内容清空只放 `Layout`）
  2. 删除 `apis/paper.js`（前端已走 `createFormulasGenerator`，HTTP 后端未启用）
  3. 删除 `utils/request.js`（仅 `apis/paper.js` 使用）
  4. 删除 `utils/download.js`（Home.vue 是唯一调用方；新流程不需要）
  5. 删除 `utils/enum.js` 的 `httpContentTypeExtensionsMappingEnum`（同上）

### A3. formData 默认值 / 配置应用 3 处重复（🔴）
- **路径**：
  - `src/views/Home.vue` 60-88
  - `src/components/Generate.vue` 102-131
  - `src/utils/configStorage.js` 27-58
- **散乱**：
  - 17 个字段在三处分别手写
  - `Generate.vue` 有 `targetMin/targetMax` 但 `Home.vue` 和 `configStorage` 部分有/部分没有
- **可复用**：
  - 抽 `src/utils/formDefaults.js` → 导出 `DEFAULT_FORM_DATA`、`DEFAULT_CONFIG_ROW`、`applyConfigToFormData(formData, config)` 单一入口
  - 调用方全部改为 `applyConfigToFormData(formData.value, config)`

### A4. paperGenerator.js 和 apis/paper.js 重复实现（🔴）
- **路径**：
  - `src/utils/paperGenerator.js` 1-65
  - `src/apis/paper.js` 1-59
- **散乱**：组装 `multiSteps / symbols / add / sub / mult / div` 的逻辑（11-39 vs 12-40）几乎逐字相同；只是 `paperGenerator` 走前端生成，`generatePaper` 走 HTTP 后端
- **可复用**：
  - 抽 `buildAutoGeneratePaperOptions(options, paperList)` → 共享
  - 然后 paperGenerator 和 generatePaper 都调用它
  - 或干脆删 apis/paper.js（若后端已废弃）

### A5. equationParser / EquationSolver / psm 之间的概念重叠（🔴）
- **路径**：
  - `src/utils/equationParser.js`（parseEquation, getCarryType, toEvalSymbols）
  - `src/utils/EquationSolver.js`（solve, checkResult, toEvalSymbols）
  - `src/utils/psm.js`（is_addcarry, is_abdication, get_num）
- **散乱**：
  - `toEvalSymbols` 同一份函数在 `equationParser.js:3-9` 和 `EquationSolver.js:1-7` 各写一遍
  - 进位/退位判断：`equationParser.getCarryType`（UI 显示）+ `psm.is_addcarry/is_abdication`（生成器校验）实现同一逻辑，命名/位置不一致
  - `getRandomNum` / `get_num` / `is_int` 在 `psm.js` 里缺乏统一封装
- **可复用**：
  - 抽 `src/utils/equationCore.js` → 集中 `toEvalSymbols / getCarryOrBorrow / lastDigit / isIntegerSafe`
  - 三个文件都 `import` 这个 core

---

## B. 重复实现（应合并到 utils/stores）

### B1. 「生成题数 > 1000」校验两处写（🟡）
- `Home.vue` 164-167、180-183
- `Generate.vue` 211-214、227-230
- **建议**：`utils/validation.js` → `validateTotalQuestions(paperList, papersPerPack, max=1000)`

### B2. paperDescriptionList 完全重复（🟡）
- `Home.vue` 118-122
- `Generate.vue` 163-167
- **建议**：抽到 `utils/paper.js` → `getPaperDescription(paper)`

### B3. selectedConfiguration 字段复制 17 行（🟡）
- `Home.vue` 132-152
- `Generate.vue` 177-199
- **建议**：见 A3 `applyConfigToFormData(formData, config)`

### B4. refreshConfiguration / addConfiguration 模式重复（🟡）
- `Home.vue` 125-131
- `Generate.vue` 169-176
- **建议**：在 `configStorage.js` 改 singleton（见 B6），调用方变成 `configStorage.loadAll()`

### B5. operatorOptions / 散落的 enum（🟡）
- `AutoGenerateFormulas.vue` 142-147：`[{key:1, label:'+'}, ...]`
- `OptionsDrawer.vue` 150-168：`whereIsResultOptions`、`remainderOptions`
- `enum.js` 只有 `fileNameGeneratedRuleEnum` + `httpContentTypeExtensionsMappingEnum`
- **建议**：把 `OPERATOR_OPTIONS`、`WHERE_IS_RESULT_OPTIONS`、`REMAINDER_OPTIONS` 都放进 `utils/enum.js`，所有组件统一 import

### B6. `new ConfigStorage().xxx()` 模式（🟡）
- 4 个文件每次 `new ConfigStorage().loadAll()` / `.save()` / `.remove()`，类没有实例状态
- **建议**：`ConfigStorage` 改为 named export 的工具对象：
  ```js
  export const configStorage = { loadAll, save, remove, clear }
  ```
  或保留类但导出单例 `export default new ConfigStorage()`

### B7. equationParser vs Database.extractOperandNumbers 解析方式不一致（🟡）
- `database.js` 5-25：`extractOperandNumbers(eq, operator)` —— 基于字符串切片
- `equationParser.parseEquation` —— 走完整规范解析
- **建议**：删 `extractOperandNumbers`，统一用 `parseEquation`，从 `parsedEquation.leftOperand / rightOperand` 取值

### B8. operatorLabel 散落（🟡）
- `stores/stats.js` 51-58：`'+': '＋', '-': '－', '*': '×', '/': '÷'`
- `components/stats/StatsDrawer.vue` 195-198：`'+': '加法', ...`
- 用途不同（一个给图表 label，一个给文字），但都叫 `operatorLabel`
- **建议**：抽 `utils/operatorMap.js` → `OPERATOR_DISPLAY = { '+': '＋', ... }` 和 `OPERATOR_NAME = { '+': '加法', ... }`

### B9. StatsDrawer 图表构造未抽（🟡）
- `StatsDrawer.vue` 212-304：`buildTrendChart()`、`buildOperatorChart()` 大段 inline
- **建议**：`composables/useChart.js` 通用 `useChartJs(refEl, configBuilder)`，自动 destroy/rebuild

### B10. Practice.vue saveSessionToDB 调用 3 次（🟡）
- 614, 683, 708 行
- 同样的 evaluation JSON 收集 → save → refresh stats
- **建议**：抽成 `composables/usePracticeSaver.js` → `saveAdaptiveGroup({ answers, engine })`

---

## C. 死代码 / 清理项

### C1. `apis/paper.js` 整文件（🟢）
- 唯一调用方是 `Home.vue`，而 Home.vue 也已废
- 整个 `utils/request.js` 仅被此文件 import
- **建议**：若后端已弃用 → 整链删

### C2. `utils/download.js`（168 行 🟢）
- 自实现的跨浏览器下载 hack
- 唯一调用方是 `Home.vue`（且没有真正调用，是死 import）
- `stores/stats.js` 的 `exportData`（179-196）已经用了现代 blob+anchor
- **建议**：删 `download.js`；若以后要导出 zip，参考 stats 写法

### C3. `utils/enum.js` 的 `httpContentTypeExtensionsMappingEnum`（🟢）
- 仅 apis/paper.js 用 → 跟随 C1 一起删

### C4. `configStorage.js` 里的 `load()`（🟢）
- 第 20 行空方法，无任何调用 → 删

### C5. `EquationSolver.checkResult`（🟡）
- `Practice.vue` 第 319 行用 `userAnswer === currentQuestion.value.solution` 直接判等
- `checkResult` 写了但未在任何 UI 路径调用
- **建议**：要么在 stats 模块（错题回放）使用，要么删；同时 `solveByBruteForce` 是兜底，正常路径不走

### C6. `get_time` 装饰器（🟢）
- `psm.js` 506-513：定义但未使用 → 删

### C7. Home.vue `debugger` 残留（🟢）
- 第 200 行 `debugger` → 删

### C8. 散落 console.log（🟢）
- `paperGenerator.js` 64：`console.log('papers', papers)`
- `Print.vue` 56, 71, 75, 80：`console.log(columnsOfPaper / timeStr / 'before' / 'after')`
- `database.js` 215：`console.log('[PracticeStore] Session saved to DB: ...')`（生产环境噪音）
- `router/index.js` 等可能也有
- **建议**：用 `if (import.meta.env.DEV) console.log(...)` 或统一 `utils/logger.js`

### C9. psm.js 旧式 author JSDoc（🟢）
- 每个函数顶都有 `Author: J.sky / Mail: bosichong@qq.com`
- 风格与项目其它新文件不一致
- **建议**：保留 1 行来源注释或合到文件头；不要每函数重复

### C10. Print.vue 文件名生成（🟢）
- 61-72 行手写 `year/month/day/hour/...` 拼字符串
- **建议**：用 `utils/timeFormat.js` 加 `formatTimestampForFilename()`，与 `formatDuration` 风格统一

---

## D. 配置 / 常量集中化

### D1. DIFFICULTY_LEVELS 与 generatePracticeConfig 概念重叠（🟡）
- `adaptiveEngine.js` 24-46：12 级难度配置（含 `formulaList / carry / abdication / resultMax`）
- `diagnostic.js` 189-273：`generatePracticeConfig(profile)` 根据 `weakLevels` 重新拼同类配置
- 两处独立维护同一概念
- **建议**：把 `DIFFICULTY_LEVELS` 作为唯一 source of truth；`generatePracticeConfig` 从 `DIFFICULTY_LEVELS` 选 `levelIdx` 派生配置

### D2. `Operators` 数字映射（🟡）
- 1=+, 2=-, 3=×, 4=÷ —— 散落在 `enum` / `psm` / `OptionsDrawer` / `AutoGenerateFormulas`
- **建议**：`utils/enum.js` → `OPERATOR = { ADD:1, SUB:2, MUL:3, DIV:4 }`

### D3. 答错反馈 / 弹窗的阈值常量（🟡）
- `adaptiveEngine.js` 247-252：`FAST=4000 / SLOW=10000 / VERY_SLOW=16000 / GOOD=0.80 / BAD=0.50 / MIN_GROUPS=2`
- `Practice.vue` 540, 624-640：评语阈值 95/80/60
- **建议**：集中到 `constants/practice.js` 或 `utils/thresholds.js`

---

## E. 小问题清单

| # | 文件 | 行 | 问题 | 建议 |
|---|---|---|---|---|
| E1 | `psm.js` | 319-325 | `getRandomBracket` `while(true)` 永真循环（理论上可能死锁）| 改 `for(let i=0; i<100; i++)` 兜底 |
| E2 | `Practice.vue` | 200 | `debugger` 残留 | 删 |
| E3 | `Print.vue` | 91-95 | `print()` 直接 `window.print()`，未与 Generate.vue `navigateToPrint` 配套 | 已有 store 调度，无需重构 |
| E4 | `stats.js` store | 156-159 | `Promise.all([loadSessions, loadAggregatedStats])` 缺错误隔离 | 拆 try/catch |
| E5 | `StatsDrawer.vue` | 200-204 | `formatDate` 内联 | 抽 `utils/timeFormat.js` |
| E6 | `database.js` | 6-25 | `extractOperandNumbers` 与 equationParser 重叠 | 改用 `parseEquation`（见 B7） |
| E7 | `Practice.vue` | 96-113 | `var h = '...'` 拼 HTML 字符串 | 改 Vue 组件 |
| E8 | `Practice.vue` | 561-576 | `window.__evalSelect` 全局桥 | 改组件 emit |
| E9 | `Generate.vue` | 81 | `import { download }` 死 import | 删 |
| E10 | `Generate.vue` | 83 | `import { generatePaper }` 死 import | 删 |
| E11 | `Generate.vue` | 49 | `import { httpContentTypeExtensionsMappingEnum }` 死 import | 删 |
| E12 | `stores/practice.js` | 5 | `LS_KEY='psm_profile'` | 集中到 `constants/storageKeys.js` |
| E13 | `stores/practice.js` | 8-25 | `loadPersistedProfile/savePersistedProfile/clearPersistedProfile` | 抽 `utils/persistedState.js` 通用工具 |
| E14 | `adaptiveEngine.js` | 168-188 | `pickInputMode` 内联概率表 | 抽 `const INPUT_MODE_PROBS = [{ L:0, p:{keypad:.7, choice2:.2, choice4:.1}}, ...]` |
| E15 | `adaptiveEngine.js` | 65-88 | `generateDistractors` | 抽到 `utils/distractors.js` 与 Practice.vue `generateOptions` 合并 |
| E16 | `Practice.vue` | 255-265 | `generateOptions` 与 `generateDistractors` 重复 | 合并（见 E15） |

---

## F. 可复用模块清单（建议沉淀位置）

| 模块 | 已有/建议新增 | 调用方 |
|---|---|---|
| `utils/equationCore.js` | **新增**：toEvalSymbols / getCarryOrBorrow / lastDigit / parseOperands | 取代 equationParser + EquationSolver 重复段 |
| `utils/formDefaults.js` | **新增**：DEFAULT_FORM_DATA / applyConfigToFormData | Home/Generate/ConfigStorage |
| `utils/validation.js` | **新增**：validateTotalQuestions | Home/Generate |
| `utils/paper.js` | **新增**：getPaperDescription / buildAutoGenerateOptions | 取代 paperGenerator / apis/paper 重复段 |
| `utils/enum.js` | **扩展**：OPERATOR / WHERE_IS_RESULT / REMAINDER | OptionsDrawer / AutoGenerateFormulas |
| `utils/operatorMap.js` | **新增**：OPERATOR_DISPLAY / OPERATOR_NAME | StatsDrawer / stats store |
| `composables/useAdaptiveSession.js` | **新增**：从 Practice.vue 抽出 | Practice.vue |
| `composables/useDisplayStrategy.js` | **新增** | Practice.vue |
| `composables/usePracticeSaver.js` | **新增** | Practice.vue |
| `composables/useChart.js` | **新增** | StatsDrawer.vue |
| `constants/practice.js` | **新增**：FAST/SLOW/THRESHOLDS/FEEDBACK_DELAYS | adaptiveEngine / Practice |
| `constants/storageKeys.js` | **新增** | stores/practice 等 |
| `utils/persistedState.js` | **新增**：localStorage 安全读写 | stores/practice |
| `components/PracticeSummaryDialog.vue` | **新增** | Practice.vue（替换内联 HTML） |
| `components/SelfEvaluationDialog.vue` | **新增** | Practice.vue（替换 window 桥） |

---

## G. 执行顺序建议（按依赖与风险）

1. **第一步（无破坏）**：抽 `utils/equationCore.js`、`utils/formDefaults.js`、`utils/validation.js`、`utils/paper.js`、`utils/enum.js` 扩展、constants/practice.js
2. **第二步（低风险）**：删 `apis/paper.js` / `utils/request.js` / `utils/download.js` / `utils/enum.js` 的 httpMapping、Home.vue 死 import 与 `debugger`
3. **第三步（中风险）**：抽 composables（`useAdaptiveSession` / `useDisplayStrategy` / `usePracticeSaver` / `useChart`），Practice.vue 改 import
4. **第四步（中风险）**：将内联 HTML 弹窗替换为独立 Vue 组件，移除 `window.__evalSelect`
5. **第五步（验收）**：跑 `npm run dev` / `npm run build`，跑通诊断 → 评估 → 自适应 → 持久化全链路；打印页正常

---

## H. 不建议动的部分

- `utils/psm.js` 主体算法（口算题生成器第三方遗产）— 仅去重 + 修永真循环，不重写
- `utils/EquationSolver.js` 的 `solveByEval` / `solveByBruteForce` — 行为正确性需 case 验证后再动
- `database.js` 的 Dexie schema — 升级到 v2 才能改字段；当前 v1 单调递增，别动
- `views/Print.vue` 的打印布局 CSS — 与浏览器 `window.print()` 紧耦合，避免重构

---

## 元信息

- **清单生成时间**：2026-06-02
- **审查范围**：src/** 全部 utils (12)、stores (3)、views (5)、components (15)、apis (1)
- **覆盖行数**：~3500 行业务代码
- **状态**：待执行
- **关联会话**：当前 WebUI session 53b06975e60b
